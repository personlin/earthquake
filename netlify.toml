[build]
  publish = "public"
  command = "hugo --minify"

[build.environment]
  HUGO_VERSION = "0.157.0"
  HUGO_ENABLEGITINFO = "true"
  NODE_VERSION = "22.x"

[context.production.environment]
  HUGO_ENV = "production"
